{% extends 'base.html.twig' %}

{% block body %}
	<h1>Commentaire</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ commentaire.id }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ commentaire.description }}</td>
			</tr>
			<tr>
				<th>DatePublication</th>
				<td>{{ commentaire.datePublication ? commentaire.datePublication|date('d-m-Y') : '' }}</td>
			</tr>
		</tbody>
	</table>
</td></tr></tbody><a href="{{ path('commentaire_index') }}">back to list</a>{{ include('commentaire/_delete_form.html.twig') }}{% endblock %}
