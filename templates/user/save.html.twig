{% extends 'base.html.twig' %}

{% block body %}

	<h1>Recettes sauvegardées</h1>
	{% for recette in app.user.recetteSauvegarde %}
		<div>{{ recette.nom }}
		</div>
	{% endfor %}

	<a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>

{% endblock %}
